name: Build rsky-relay

on:
  push:
    branches: [main]
    paths:
      - 'rsky-relay/**'
      - 'rsky-common/**'
      - 'rsky-crypto/**'
      - 'rsky-syntax/**'
      - 'rsky-identity/**'
      - 'rsky-lexicon/**'
      - 'rsky-firehose/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-rsky-relay.yml'
  workflow_dispatch:

env:
  IMAGE: ghcr.io/know-me-tools/rsky-relay

jobs:
  build:
    name: Build rsky-relay
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=sha,prefix=,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - uses: docker/setup-buildx-action@v3

      - uses: docker/build-push-action@v5
        with:
          context: .
          file: rsky-relay/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=rsky-relay
          cache-to: type=gha,mode=max,scope=rsky-relay
