# Tasks: prod-c017

- [x] Fix `post_view_with_author` in `appview-api/src/feed.rs` to populate `record` from `row.text` and `row.created_at`
- [x] Fix `post_view_plain` in `appview-api/src/feed.rs` to populate `record` from `row.text` and `row.created_at`
- [x] Run `cargo check -p appview-api` to verify compilation
