# Tasks: p1-c006

- [ ] Add `rsky-relay` entry to build matrix in `.github/workflows/deploy.yaml`
- [ ] Add `Inject rsky-relay secret` step to `inject-secrets` job
- [ ] Add `Inject rsky-feedgen secret` step to `inject-secrets` job
- [ ] Add `Inject rsky-labeler secret` step to `inject-secrets` job
- [ ] Add `Inject rsky-jetstream-subscriber secret` step to `inject-secrets` job
- [ ] Update `k8s/README.md` — new secrets table rows (RELAY_ADMIN_PASSWORD, RSKY_API_KEY, MOD_SERVICE_*)
- [ ] Update `k8s/README.md` — add labeler activation instructions section
