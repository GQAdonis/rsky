apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: atproto
  labels:
    app: pgbouncer
spec:
  selector:
    app: pgbouncer
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP
