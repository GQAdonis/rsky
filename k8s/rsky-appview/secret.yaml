apiVersion: v1
kind: Secret
metadata:
  name: rsky-appview-secrets
  namespace: atproto
type: Opaque
stringData:
  DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer:5432/rsky"
  RELAY_HOSTS: "${APPVIEW_RELAY_HOSTS:-wss://relay.know-me.tools}"
  SERVICE_DID: "did:web:appview.know-me.tools"
  SERVICE_HOSTNAME: "appview.know-me.tools"