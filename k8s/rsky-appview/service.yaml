apiVersion: v1
kind: Service
metadata:
  name: rsky-appview
  namespace: atproto
  labels:
    app: rsky-appview
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: rsky-appview