apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: rsky-appview
  namespace: atproto
  labels:
    app: rsky-appview
spec:
  selector:
    matchLabels:
      app: rsky-appview
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
