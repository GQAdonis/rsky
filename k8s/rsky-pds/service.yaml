apiVersion: v1
kind: Service
metadata:
  name: rsky-pds
  namespace: atproto
  labels:
    app: rsky-pds
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: rsky-pds
