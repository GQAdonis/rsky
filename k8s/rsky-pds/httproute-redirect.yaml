apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rsky-pds-http-redirect
  namespace: atproto
spec:
  parentRefs:
    - name: pds-gateway
      sectionName: http
  hostnames:
    - pds.know-me.tools
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
