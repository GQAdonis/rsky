apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: feedgen-gateway
  namespace: atproto
spec:
  gatewayClassName: eg
  listeners:
    - name: https
      protocol: HTTPS
      port: 443
      hostname: feed.know-me.tools
      tls:
        mode: Terminate
        certificateRefs:
          - name: wildcard-know-me-tools-tls
            kind: Secret
    - name: http
      protocol: HTTP
      port: 80
      hostname: feed.know-me.tools
