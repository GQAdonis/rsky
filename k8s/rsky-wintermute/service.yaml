apiVersion: v1
kind: Service
metadata:
  name: rsky-wintermute
  namespace: atproto
  labels:
    app: rsky-wintermute
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: rsky-wintermute
