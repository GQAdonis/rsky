apiVersion: v1
kind: Secret
metadata:
  name: rsky-wintermute-secrets
  namespace: atproto
type: Opaque
stringData:
  DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer:5432/rsky"
  RELAY_HOSTS: "ws://rsky-relay:9000"
